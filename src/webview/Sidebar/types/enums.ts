export enum Views {
  CONNECTION = "CONNECTION",
  SCHEMA = "SCHEMA",
  QUERY = "QUERY",
  OUTPUT = "OUTPUT",
}

export enum DBType {
  POSTGRES = "postgres",
  MYSQL = "mysql",
  SQLITE = "sqlite",
  MSSQL = "mssql",
  ORACLE = "oracle",
}